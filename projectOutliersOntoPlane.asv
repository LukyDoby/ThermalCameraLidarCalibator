function cloudOut = projectOutliersOntoPlane(cloud, coeffs, inliers,thresh)
    N =[coeffs(1), coeffs(2), coeffs(3)]; % normal vector

    a = 1;
    b = length(inliers);
    r = round((b-a).*rand + a);

    P0 = inliers(r,:); % random point on the plane
    
    for i = 1:length(cloud.Location)
        P = cloud.Location(i,:);
        distances = abs(N(1) * P(:,1) + N(2) * P(:,2) + N(3) * P(:,3) + N(4)) / sqrt(N(^2 + B^2 + C^2);
        
    end

end